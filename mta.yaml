_schema-version: "3.2"
ID: comnsdmsfioriapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comnsdmsfioriapp-destination-content
  type: com.sap.application.content
  requires:
  - name: comnsdmsfioriapp-destination-service
    parameters:
      content-target: true
  - name: comnsdmsfioriapp-repo-host
    parameters:
      service-key:
        name: comnsdmsfioriapp-repo-host-key
  - name: comnsdmsfioriapp-uaa
    parameters:
      service-key:
        name: comnsdmsfioriapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comnsdmsfioriapp_html_repo_host
          ServiceInstanceName: comnsdmsfioriapp-html5-srv
          ServiceKeyName: comnsdmsfioriapp-repo-host-key
          sap.cloud.service: comnsdmsfioriapp
        - Authentication: OAuth2UserTokenExchange
          Name: comnsdmsfioriapp_uaa
          ServiceInstanceName: comnsdmsfioriapp-xsuaa-srv
          ServiceKeyName: comnsdmsfioriapp-uaa-key
          sap.cloud.service: comnsdmsfioriapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comnsdmsfioriapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comnsdmsfioriapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comnsdmsfioriapp.zip
      name: comnsdmsfioriapp
      target-path: resources/
- name: comnsdmsfioriapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comnsdmsfioriapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comnsdmsfioriapp-destination-service
    service-plan: lite
- name: comnsdmsfioriapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comnsdmsfioriapp-xsuaa-srv
    service-plan: application
- name: comnsdmsfioriapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comnsdmsfioriapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
